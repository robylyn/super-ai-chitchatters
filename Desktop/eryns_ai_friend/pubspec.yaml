name: eryns_ai_friend
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^5.0.1
  cupertino_icons: ^1.0.8
  firebase_auth: ^5.1.0
  firebase_core: ^3.1.0
  flutter_lints: ^4.0.0
  tflite_flutter: ^0.10.4
  image_picker: ^1.1.2
  image: ^4.2.0
  camera: ^0.11.0+1
  collection: 1.18.0  # Pinned to version compatible with flutter_test
  ffi: ^2.1.2
  meta: 1.12.0  # Pinned to version compatible with flutter_test
  path_provider: ^2.1.3
  # Add other dependencies here as needed

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/model.tflite
    - assets/
